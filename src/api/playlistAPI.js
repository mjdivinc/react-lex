export async function playlistAPI() {
    // all client side so this is a little irrelevant
    /* const response = await fetch(`http://localhost:3000/login`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            savedCourses
        })
    });
 */

    const response = {
        "status": 200
    }
    return response;
}